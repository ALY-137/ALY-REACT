{
  "name": "aly-react",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "dompurify": "^3.4.3",
    "firebase": "^9.23.0",
    "jwt-decode": "^4.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "audit": "npm audit",
    "audit:fix": "npm audit fix",
    "discord:bot": "node scripts/discord-bot.mjs",
    "discord:notify": "node scripts/discord-notify.mjs",
    "discord:queue": "node scripts/discord-queue.mjs",
    "firestore:rules:deploy": "npx firebase-tools deploy --only firestore:rules",
    "firestore:rules:deploy:portable": "node scripts/deploy-firestore-rules.js",
    "storage:rules:deploy": "npx firebase-tools deploy --only storage",
    "firestore:emulators": "npx firebase-tools emulators:start --only firestore",
    "functions:install": "npm --prefix functions install",
    "functions:deploy": "npx firebase-tools deploy --only functions"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
